export type ToolboxIcon = {
  id: string;
  name: string;
  url: string;
  coverType: "color" | "image";
  coverColor?: string;
  coverImageUrl?: string;
  x: number;
  y: number;
  createdAt: string;
  updatedAt: string;
};
